mutation ($input: CreateOrUpdateCorporationInput!) {
    createOrUpdateCorporation(input: $input) {
        id,
        name,
        country {
            name,
            en,
            code,
        }
    }
}
