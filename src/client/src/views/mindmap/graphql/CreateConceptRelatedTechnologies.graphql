mutation($conceptId: BigInt!, $technologyIds: [BigInt!]!) {
    createConceptRelatedTechnologies(
        conceptId: $conceptId,
        technologyIds: $technologyIds
    ) {
        success
        message
    }
}
